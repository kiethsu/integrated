<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="assets/favicon.ico" type="image/x-icon"> <!-- Link to the ICO file -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Fira+Sans:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <title>Login</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      height: 100vh;
      background-color: #8fd0b3a8;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      width: 80%;
      height: 80%;
      max-width: 1200px;
      max-height: 800px;
      padding: 20px;
      background-color: #4e000000;
    }

    .video-section {
      width: 65%;
      height: 82vh;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    .video-section video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      box-shadow: 10px 10px 10px 5px rgba(0, 0, 0, 0.5);
      z-index: 15;
      border-radius: 10px;
    }

    .form-container {
      width: 45%;
      height: 80vh;
      padding-left: 90px;
      padding-right: 80px;
      background-color: #fffdf6;
      box-shadow: 8px 8px 30px rgba(0, 0, 0, 0.3);
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: flex-start;
      border-radius: 0px 20px 20px 0;
    }

    .return-link {
      align-self: flex-start;
      margin-top: 20px;
    }

    .return-link a {
      text-decoration: none;
      color: blue;
      font-weight: bold;
    }

    .return-link a:hover {
      text-decoration: underline;
    }

    form {
      display: flex;
      flex-direction: column;
      width: 100%;
      margin-top: 20px;
    }

    .input-container {
      position: relative;
      margin-bottom: 30px;
      border: 2px solid transparent; /* Make border transparent initially */
      border-radius: 5px; /* Rounded corners for the input container */
      transition: border-color 0.3s; /* Smooth transition for border color */
    }


    .floating-label {
      position: absolute;
      top: 14px;
      left: 14px;
      font-size: 18px;
      color: #999;
      transition: 0.2s ease all;
    }

    input {
      padding: 14px;
      border-radius: 5px;
      width: 100%;
      box-sizing: border-box;
      border-width: .5px;
    }

    input:focus + .floating-label,
    input:not(:placeholder-shown) + .floating-label {
      top: -25px;
      left: -14px;
      font-size: 20px;
      color: #000000;
    }

    button {
      padding: 20px;
      border: none;
      background-color: #004aad; /* Initial background color */
      color: white; /* Text color */
      font-size: 18px; /* Font size */
      font-weight: 500;
      font-family: 'Fira sans', sans-serif; /* Font family */
      border-radius: 10px; /* Rounded corners */
      cursor: pointer; /* Pointer cursor on hover */
      transition: background-color 0.3s, transform 0.3s, box-shadow 0.3s; /* Smooth transition */
      transition: transform 0.3s ease, box-shadow 0.3s ease; /* Add transition for hover effect */
      }

    .services {
      padding: 8px;
      border: none;
      background-color: #004aad; /* Initial background color */
      color: white; /* Text color */
      font-size: 10px; /* Font size */
      font-family: 'Fira sans', sans-serif; /* Font family */
      border-radius: 10px; /* Rounded corners */
      cursor: pointer; /* Pointer cursor on hover */
      transition: background-color 0.3s, transform 0.3s, box-shadow 0.3s; /* Smooth transition */
    }

    .services:hover {
      transform: translateY(-5px); /* Move button up on hover */
      box-shadow: 0px 8px 12px rgba(0, 0, 0, 0.3); /* Increase shadow on hover */
    }

    .icon-chevron-right-services,
    .icon-chevron-right2-services {
      width: 10px; /* Icon width */
      height: 10px; /* Icon height */
      transition: transform 0.3s ease; /* Add transition for icon movement */
      transform: rotate(180deg); /* Rotate the icon by 180 degrees */
    }

    .services:hover .icon-chevron-right-services,
    .services:hover .icon-chevron-right2-services {
      animation: moveLeft 0.8s linear infinite; /* Continuous movement when hovered */
    }

    @keyframes moveLeft {
      0% {
        transform: translateX(0) rotate(180deg); /* Start at the original position and rotated */
      }
      50% {
        transform: translateX(-8px) rotate(180deg); /* Move the icons to the left while maintaining rotation */
      }
      100% {
        transform: translateX(0) rotate(180deg); /* Move back to the original position while maintaining rotation */
      }
    }

    button:hover {
      background-color: #004aad; /* Change background on hover */
      transform: translateY(-4px); /* Lift the button slightly */
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3); /* Add shadow for depth */
    }


    .bottom-link {
      margin-top: 20px;
      text-align: center;
      font-size: 14px; /* Font size */
      font-family: 'Fira sans', sans-serif; /* Font family */
    }

    .bottom-link a {
      text-decoration: none;
      color: blue;
      font-size: 14px; /* Font size */
      font-weight: 500;
      font-family: 'Fira sans', sans-serif; /* Font family */
    }

    .bottom-link a:hover {
      text-decoration: underline;
    }

    .password-container {
      display: flex;
      align-items: center;
      position: relative;
    }

    .password-container input[type="password"] {
      flex: 1;
      padding-right: 40px;
    }

    .toggle-password {
      cursor: pointer;
      position: absolute;
      right: 5px;
      top: 50%;
      transform: translateY(-50%);
      opacity: 30%;
    }

    .toggle-password i {
      font-size: 25px;
    }

    .signin span {
      color: #000000;
      font-family: 'Fira sans', sans-serif;  
      font-size: 70px;
      font-weight: 800;
      text-align: left;
      display: block;
      margin-bottom: 20px;
      padding-top: 130px;
      padding-bottom: 30px;
    }

    .overlay-container {
      position: absolute;
      top: 27%; /* Centers vertically based on viewport height */
      left: 76%; /* Centers horizontally */
      transform: translate(-86%, -20%); /* Ensures the container stays centered */
      z-index: 16;
      width: 100%; /* Percentage-based width for responsive scaling */
      max-width: 400px; /* Adds a max-width to control excessive growth on large screens */
      padding: 2%; /* Responsive padding */
      background: rgba(255, 255, 255, 0.041);
      backdrop-filter: blur(3px);
      border-radius: 10px;
      display: flex;
      flex-direction: column;
      align-items: center;
      color: #ffffff;
      text-align: center;
      height: 60%;
      border: solid 1px;
  }

  .overlay-container img {
    width: 70%; /* Makes the image responsive within container */
    height: auto;
    margin-bottom: 3%;
  }

  .overlay-container h2 {
    font-size: 2vw;
    max-font-size: 30px;
    margin: 0;
    font-family: 'Fira Sans', sans-serif;
  }

  .overlay-container p {
    font-size: 1vw;
    max-font-size: 20px;
    font-family: 'Roboto', sans-serif;
    margin: 2% 0;
  }
  /* Responsive adjustments */
@media (max-width: 1800px) and (min-width: 720px) {
  .container {
  display: column;
  justify-content: space-between;
  align-items: center;
  width: 80%;
  height: 80%;
  max-width: 800px;
  max-height: 800px;
  padding: 20px;
  background-color: #4e000000;
  }

  .video-section {
    width: 70%;
    height: 100%;
  }

  .video-section video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  box-shadow: 10px 10px 10px 5px rgba(0, 0, 0, 0.5);
  z-index: 15;
  border-radius: 10px;
}

  .form-container {
    width: 40%;
    padding-left: 40px;
    padding-right: 40px;
    height: 95%;
  }

  .return-link {
  align-self: flex-start;
  margin-top: 10px;
}

.return-link a {
  text-decoration: none;
  color: blue;
  font-weight: bold;
}

.return-link a:hover {
  text-decoration: underline;
}

  .input-container {
    margin-bottom: 25px;
  }

  .floating-label {
  position: absolute;
  top: 8px;
  left: 8px;
  font-size: 12px;
  color: #999;
  transition: 0.2s ease all;
}

input {
  padding: 14px;
  border-radius: 5px;
  width: 100%;
  box-sizing: border-box;
  border-width: .5px;
}

input:focus + .floating-label,
input:not(:placeholder-shown) + .floating-label {
  top: -25px;
  left: -14px;
  font-size: 12px;
  color: #000000;
}

  input {
    padding: 5px;
  }

  button {
    padding: 10px;
    font-size: 12px;
  }

  .services {
    font-size: 12px;
    padding: 6px;
  }

  .bottom-link {
    font-size: 8px;
  }

  .bottom-link a {
      text-decoration: none;
      color: blue;
      font-size: 10px; /* Font size */
      font-weight: 500;
      font-family: 'Fira sans', sans-serif; /* Font family */
    }

  .signin span {
    font-size: 35px;
    padding-top: 50px;
  }

  .toggle-password {
  cursor: pointer;
  position: absolute;
  right: 5px;
  top: 50%;
  transform: translateY(-50%);
  opacity: 30%;
}

.toggle-password i {
  font-size: 16px;
}
.services {
      padding: 6px;
      border: none;
      background-color: #004aad; /* Initial background color */
      color: white; /* Text color */
      font-size: 8px; /* Font size */
      font-family: 'Fira sans', sans-serif; /* Font family */
      border-radius: 10px; /* Rounded corners */
      cursor: pointer; /* Pointer cursor on hover */
      transition: background-color 0.3s, transform 0.3s, box-shadow 0.3s; /* Smooth transition */
    }

.overlay-container {
  position: absolute;
    top: 27%; /* Centers vertically based on viewport height */
    left: 79%; /* Centers horizontally */
    transform: translate(-86%, -20%); /* Ensures the container stays centered */
    z-index: 16;
    width: 75%; /* Percentage-based width for responsive scaling */
    max-width: 400px; /* Adds a max-width to control excessive growth on large screens */
    padding: 2%; /* Responsive padding */
    background: rgba(255, 255, 255, 0.041);
    backdrop-filter: blur(3px);
    border-radius: 10px;
    display: flex;
    flex-direction: column;
    align-items: center;
    color: #ffffff;
    text-align: center;
    height: 70%;
    border: solid 1px;
  }

  .overlay-container img {
    width: 70%; /* Makes the image responsive within container */
    height: auto;
    margin-bottom: 3%;
  }

  .overlay-container h2 {
    font-size: 2vw;
    max-font-size: 30px;
    margin: 0;
    font-family: 'Fira Sans', sans-serif;
  }

  .overlay-container p {
    font-size: 1vw;
    max-font-size: 20px;
    font-family: 'Roboto', sans-serif;
    margin: 2% 0;
    
  }
}


  </style>

  <script>
    function togglePassword() {
      const passwordInput = document.getElementById('password');
      const eyeIcon = document.getElementById('eyeIcon');

      // Toggle password visibility
      if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        eyeIcon.classList.remove('fa-eye');
        eyeIcon.classList.add('fa-eye-slash');
      } else {
        passwordInput.type = 'password';
        eyeIcon.classList.remove('fa-eye-slash');
        eyeIcon.classList.add('fa-eye');
      }
    }

    // Add event listeners to input fields for border color
    document.addEventListener('DOMContentLoaded', () => {
      const inputContainers = document.querySelectorAll('.input-container');

      inputContainers.forEach(container => {
        const input = container.querySelector('input');
        
        // Add focus event listener
        input.addEventListener('focus', () => {
          container.classList.add('active');
        });

        // Add blur event listener
        input.addEventListener('blur', () => {
          if (!input.value) {
            container.classList.remove('active');
          }
        });
      });
    });
  </script>
</head>
<body>
  <div class="container">

    <div class="video-section">
      <div class="overlay-container">
        <img src="/assets/logo-casa-2-20.png" alt="Overlay Image">
        <h2>Welcome to Casa Animalia Website</h2>
        <p>Signin to add your appointment</p>
      </div>
      <video autoplay loop muted playsinline>
        <source src="/assets/loginstock.mp4" type="video/mp4">
        Your browser does not support the video tag.
      </video>

      
    </div>
    <div class="form-container">
      <div class="return-link">
          <button class="services" onclick="location.href='/'" id="ourteam">    
            <img class="icon-chevron-right-services" src="./assets/icon-chevron-right0.svg" />
            <img class="icon-chevron-right2-services" src="./assets/icon-chevron-right1.svg" />
            Return to Homepage
   </button> 
      </div>
      <div class="signin">
        <span>Signin</span>
      </div>
    
      <form action="/login" method="POST">
        <div class="input-container">
          <input type="email" id="email" name="email" required placeholder=" ">
          <label for="email" class="floating-label">Email</label>
        </div>
    
        <div class="input-container password-container">
          <input type="password" id="password" name="password" required placeholder=" ">
          <label for="password" class="floating-label">Password</label>
          <span class="toggle-password" onclick="togglePassword()">
            <i class="fa fa-eye" id="eyeIcon"></i>
          </span>
        </div>
        
        <button type="submit">LOGIN</button>
      </form>
    
      <div class="bottom-link">
        Don't have an account? <a href="/register">REGISTER HERE</a>
      </div>
    </div>
  </div>
</body>
</html>